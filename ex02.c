#include <stdio.h>

int main()
{
    
    //VARIABILI//
    int a; 
    float b = 2.512; //%f
    double c = 2.612; //%lf
    //%c ???
    a = 5;
    printf("somma: %f\n", a+b); // identificatore per int: %d
    float differenza = a-b;
    printf("differnza: %f\n", differenza);
    float divisione = a/b;
    printf("divisione: %f\n", divisione); 
    
    //SOMMA? DIFFERENZA?// è possibile effettuare operazioni tra variabili diverse?
}