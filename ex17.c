#include <stdio.h>
int main()
{
    int a;
    int n = N+1;
    int c=c+1;
    while (a <= 100)
    {
        printf("%d\n", a);
        a = a + 1;
        if(n%a==0)
        printf(non è un numero primo);
    
    }
}